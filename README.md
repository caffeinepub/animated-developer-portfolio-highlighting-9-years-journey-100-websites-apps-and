## Animated developer portfolio highlighting 9+ years journey, 100+ websites, apps, and digital services

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
